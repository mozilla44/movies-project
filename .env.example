# These values are read only by the local Vite proxy and the Netlify function.
# Never prefix a secret with VITE_, because Vite exposes those values to browsers.
TMDB_READ_ACCESS_TOKEN="your_tmdb_v4_read_access_token"
# TMDB_API_KEY="your_tmdb_v3_api_key"
